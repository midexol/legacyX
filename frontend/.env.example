# Copy this file to .env and fill in once the backend is deployed.
# Until this is set, the app runs on local mock data automatically —
# no backend needed for frontend development.

VITE_API_BASE_URL=http://localhost:4000
